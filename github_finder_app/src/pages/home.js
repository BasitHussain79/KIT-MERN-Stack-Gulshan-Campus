import React, { Fragment } from 'react';
import { HomeDefault } from '../components/features';

const Home = () => {
  return (
    <Fragment>
      <HomeDefault />
    </Fragment>
  );
};

export default Home;
